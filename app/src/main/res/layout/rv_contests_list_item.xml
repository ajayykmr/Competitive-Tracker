<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    android:layout_marginVertical="4dp"
    app:cardCornerRadius="10dp"
    android:elevation="10dp"
    app:cardBackgroundColor="@color/secondary"

    >

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="12dp"
        >

        <TextView
            android:id="@+id/tv_contest_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"

            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"

            android:text="2023 ICPC Post-World Finals Challenge powered by Huawei"
            android:textSize="18sp"
            android:ellipsize="end"
            android:maxWidth="225dp"
            />

        <TextView
            android:layout_marginTop="4dp"
            android:id="@+id/tv_contest_date"
            android:layout_width="0dp"
            android:layout_height="wrap_content"


            app:layout_constraintEnd_toEndOf="parent"

            app:layout_constraintTop_toTopOf="@id/tv_contest_name"
            app:layout_constraintBottom_toBottomOf="@id/tv_contest_name"
            app:layout_constraintStart_toEndOf="@id/tv_contest_name"
            android:layout_marginLeft="8dp"
            android:textAlignment="textEnd"
            android:text="DATE"


            android:textSize="14sp"
            />


    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>